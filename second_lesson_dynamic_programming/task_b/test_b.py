import pytest
from io import StringIO
import sys
from b import main


@pytest.mark.parametrize('input_data, answer', [
      # 1️⃣ Базовый пример из условия
    ('LLBLRRBRL', 5),

    # 3️⃣ Все притоки с левого берега — переправляемся 1 раз в начале
    ('LLLLL', 1),

    # 4️⃣ Все притоки с правого берега — нужно перейти на правый берег (1 переправа), а потом назад не возвращаемся
    ('RRRRR', 1),

    # 5️⃣ Все притоки с обоих берегов (симметрично) — придётся несколько раз менять сторону
    ('BBBBB', 6),  # оба берега доступны, можно идти без переправ

    # 6️⃣ Чередование левого и правого притока — частые смены стороны
    ('LRLRLR', 4),  # нужно каждый раз переправляться

    # 7️⃣ Чередование с участием B — упрощает движение (B не требует переправы)
    ('LBRBRL', 5),

    # 8️⃣ Сложная комбинация — ручная проверка
    ('LLBLRRBRL', 5),

    # 9️⃣ Случай с "мостами" (B) в начале и конце
    ('BLLRBBRL', 6),

    # 🔟 Крайний случай максимальной длины (все символы одинаковы, 200 символов)
    ('L' * 200, 1),
])
def test_h(input_data, answer):
    sys.stdin = StringIO(input_data)
    
    result = main()
    assert result == answer

